meta {
  name: Search documents by keyword
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/documents?familyMemberId={{memberId}}&keyword=เบาหวาน
  body: none
  auth: none
}

headers {
  x-line-userid: {{ownerUserId}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
