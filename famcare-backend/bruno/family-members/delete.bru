meta {
  name: Delete family member (soft delete)
  type: http
  seq: 98
}

delete {
  url: {{baseUrl}}/api/v1/family-members/{{memberId}}
  body: none
  auth: none
}

headers {
  x-line-userid: {{ownerUserId}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
