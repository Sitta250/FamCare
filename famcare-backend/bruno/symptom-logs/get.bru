meta {
  name: Get symptom log
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/symptom-logs/{{symptomLogId}}
  body: none
  auth: none
}

headers {
  x-line-userid: {{ownerUserId}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
