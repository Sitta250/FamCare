meta {
  name: Delete health metric
  type: http
  seq: 95
}

delete {
  url: {{baseUrl}}/api/v1/health-metrics/{{metricId}}
  body: none
  auth: none
}

headers {
  x-line-userid: {{ownerUserId}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
