meta {
  name: Delete BLOOD_PRESSURE threshold
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/health-metrics/{{memberId}}/thresholds/BLOOD_PRESSURE
  body: none
  auth: none
}

headers {
  x-line-userid: {{ownerUserId}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
